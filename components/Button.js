const Button = ({ title }) => {
  return (
    <button className="font-bold font-main text-base text-[#1D1E22] bg-[#0DB760] py-4 px-7 rounded-md">
      {title}
    </button>
  );
};

export default Button;
